%ul
  - @departments.each do |department|
    %li
      %h3= department.name
    %ul
      - department.positions.each do |position|
        %li
          = position.name
          %ul
            - if @pending_interviews.keys.include? position
              - @pending_interviews[position].each do |interview|
                %li= link_to interview.information, interview.application_record
            - else
              No pending interviews
