%h1 Log in
= button_to 'Log in with Microsoft', main_app.azure_login_path, class: 'btn btn-primary'
%h2 Development login
= form_with url: main_app.auth_callback_path(provider: 'developer'), method: :get do |f|
  = f.collection_select :uid, User.all, :entra_uid, :full_name
  = f.submit 'Log in'
