%h1 Edit settings for #{@user.full_name}

= render 'form', locals: {user: @user, action: :update}

= form_tag controller: :users,
           action: :destroy,
           method: :delete,
           id: @user.id do
  = submit_tag "Remove #{@user.full_name}"
