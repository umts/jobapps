Hello,

An application which you had saved for later has been moved back to the dashboard as scheduled. It is now ready for your review.

Position: <%= @record.position.name %>.
Applicant: <%= @record.user.full_name %>.
Applied on: <%= @record.created_at.to_formatted_s :long_with_time %>.
<%- if @record.note_for_later.present? %>
Note: <%= @record.note_for_later %>
<%- end %>
