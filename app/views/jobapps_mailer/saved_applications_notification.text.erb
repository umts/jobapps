Hello,

The following applications have been reactivated:
<%= @info.each_key do |position|%>
  Position: <%= position %>
  <%= @info[position].each do |record|%>
    Applicant: <%= record.user.full_name %>,
    Date Applied: <%= record.created_at.strfime('%m/%d/%Y %H:%M') %>
  <%= end %>
<%= end %>