Hello <%= @user.first_name %>,

<% if configured_value [:on_application_denial, :notify_of_reason], default: true %>
  Your application to the <%= @filed_application.position.try(:name) %> position has been denied due to the following reason:
  <%= @filed_application.staff_note %>
<% else %>
  Your application has been denied.
<% end %>

Best,

<%= configured_value [:organization_name] %>
